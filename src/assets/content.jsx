import React from 'react'
import Search from './search'

const content = () => {
    const {currentNumber} = Search(5879);

  return (
    <div>
    
    </div>
  )
}

export default content
